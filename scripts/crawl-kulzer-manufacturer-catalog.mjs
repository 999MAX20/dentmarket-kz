import fs from "node:fs/promises";
import path from "node:path";
const sitemapUrl = "https://kulzer.com/en/en/xml-sitemap.xml"; const jsonOutputPath = path.resolve("data/catalog-evidence/kulzer-manufacturer-catalog.json"); const queueOutputPath = path.resolve("data/curation/kulzer-manufacturer-catalog-queue.csv");
const clean = (value) => String(value ?? "").replace(/<[^>]*>/gu, " ").replaceAll("&amp;", "&").replaceAll("&reg;", "®").replaceAll("&trade;", "™").replace(/\s+/gu, " ").trim(); const escapeCsv = (value) => { const text = String(value ?? ""); return /[",\n\r]/u.test(text) ? `"${text.replaceAll('"', '""')}"` : text; }; const fetchText = async (url) => { let error; for (let attempt = 0; attempt < 2; attempt += 1) { try { const response = await fetch(url, { signal: AbortSignal.timeout(30_000), headers: { "user-agent": "DentMarket-KZ catalog evidence audit/1.0" } }); if (!response.ok) throw new Error(`${response.status} ${response.statusText}`); return response.text(); } catch (caught) { error = caught; } } throw error; };
const sitemap = await fetchText(sitemapUrl); const urls = [...new Set([...sitemap.matchAll(/<loc>([^<]+)<\/loc>/gu)].map((match) => clean(match[1])).filter((url) => /\/en\/en\/products\/[^/]+\.html$/u.test(url)))]; const products = [], errors = []; let cursor = 0; const worker = async () => { while (cursor < urls.length) { const sourcePageUrl = urls[cursor++]; try { const html = await fetchText(sourcePageUrl); const name = clean(html.match(/<meta name="og:title" content="([^"]+)"/iu)?.[1] || html.match(/<title>([\s\S]*?)<\/title>/iu)?.[1]); const sourceImageUrl = clean(html.match(/<meta name="og:image" content="([^"]+)"/iu)?.[1]); const description = clean(html.match(/<meta name="description" content="([^"]*)"/iu)?.[1]); if (!name || /error 404/iu.test(name)) throw new Error("active product name not found"); const slug = sourcePageUrl.split("/").at(-1).replace(/\.html$/u, ""); products.push({ officialProductId: `KULZER-${slug.toUpperCase()}`, brand: "Kulzer", manufacturer: "Kulzer GmbH", name, manufacturerRef: "", manufacturerRefs: "", model: name, variantCount: 1, variants: [{ variantLabel: name, manufacturerRef: "", status: "MANUFACTURER_REFERENCE_REQUIRED" }], categoryPath: "Kulzer / Dental products", description, sourceImageUrl, imageUrls: sourceImageUrl, imageCount: sourceImageUrl ? 1 : 0, sourcePageUrl, additionalSourceUrls: sitemapUrl, kzEvidence: "BRAND_AND_SELECTED_PRODUCTS_OBSERVED_IN_KAZAKHSTAN_EXACT_SKU_REVIEW_REQUIRED", status: sourceImageUrl ? "MANUFACTURER_REFERENCE_REQUIRED" : "PHOTO_REQUIRED" }); } catch (error) { errors.push({ sourcePageUrl, error: String(error?.message ?? error) }); } } }; await Promise.all(Array.from({ length: 8 }, () => worker())); products.sort((a, b) => a.name.localeCompare(b.name, "en")); const report = { brand: "Kulzer", manufacturer: "Kulzer GmbH", sourceType: "CURRENT_OFFICIAL_ENGLISH_PRODUCT_SITEMAP", sourceUrl: sitemapUrl, lastChecked: new Date().toISOString().slice(0, 10), policy: { allOfficialEnglishProductPagesCollected: true, exactOfficialImagesPreserved: true, noManufacturerReferenceInvented: true, missingFieldsNeverDeleteCard: true }, totals: { sitemapProductPages: urls.length, discoveredProducts: products.length, variantSkus: products.length, productsWithImages: products.filter((product) => product.imageCount > 0).length, productsWithReferences: 0, crawlErrors: errors.length }, errors, products }; const headers = ["officialProductId", "brand", "manufacturer", "name", "manufacturerRef", "manufacturerRefs", "variantCount", "categoryPath", "sourceImageUrl", "imageCount", "kzEvidence", "status", "sourcePageUrl"]; const csv = [headers.join(","), ...products.map((product) => headers.map((header) => escapeCsv(product[header])).join(","))].join("\n") + "\n"; await fs.mkdir(path.dirname(jsonOutputPath), { recursive: true }); await fs.mkdir(path.dirname(queueOutputPath), { recursive: true }); await Promise.all([fs.writeFile(jsonOutputPath, JSON.stringify(report, null, 2) + "\n"), fs.writeFile(queueOutputPath, csv)]); console.log(JSON.stringify(report.totals, null, 2));
